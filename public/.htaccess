Options -Indexes
RewriteEngine On
# Laragon subfolder. Jika DocumentRoot = public/, ganti menjadi: RewriteBase /
RewriteBase /notepad-share/public/

RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

RewriteRule ^ index.php [L]
